machine:
  java:
    version: openjdk7

general:
  artifacts:
    - "build/libs/pipeline-view.hpi"

dependencies:
  override:
    - ./gradlew resolveAllDependencies

test:
  override:
    - ./gradlew --offline build

dependencies:
  cache_directories:
    - "node_modules/"
